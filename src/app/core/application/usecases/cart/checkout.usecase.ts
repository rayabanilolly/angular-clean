// import { inject, Injectable } from '@angular/core';
// import { CART_REPOSITORY_TOKEN } from '../../outputs/cart-api.output';
// import { CheckoutInput } from '../../inputs/checkout.input';

// @Injectable()
// export class CheckoutCartUseCase implements CheckoutInput {
//      private readonly cart = inject(CART_REPOSITORY_TOKEN);

//     async checkout(cartId: string): Promise<void> {
//         await this.orderService.checkout(cartId);
//     }
// }
